# @package _global_

defaults:
  - override /model: co2emiss_cnnlstm
  - override /data/output: inversion
  - override /data/input: inversion
  - override /data/path: all_but_box_reduced

data:
  init:
    shift: 1